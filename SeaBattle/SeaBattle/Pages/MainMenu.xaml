<UserControl x:Class="SeaBattle.Pages.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid x:Name="mainMenuPage" Background="White">
        <TextBlock x:Name="mainMenuTextBlock" 
                   Margin="0,0,0,330"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   Width="Auto" 
                   Height="30" 
                   Text="Главное меню" 
                   TextWrapping="Wrap" 
                   FontSize="20"
                   FontWeight="Bold"/>        
        <TextBlock x:Name="backTextBlock" 
                   Style="{StaticResource ScaleTextBlockOnFocus}"
                   Text="&lt; Назад"
                   Margin="20,20,0,0"
                   HorizontalAlignment="Left"  
                   VerticalAlignment="Top"
                   Width="70"
                   Height="30"   
                   FontSize="16"
                   MouseLeftButtonDown="backTextBlock_MouseLeftButtonDown">
            <TextBlock.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform/>
                </TransformGroup>
            </TextBlock.RenderTransform>
        </TextBlock>
        <Button x:Name="newGameButton"
                Content="Новая игра"
                Margin="0,0,0,210"  
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Width="200" 
                Height="30" 
                FontSize="18" Click="newGameButton_Click"/>
        <Button x:Name="connectionGameButton"
                Content="Подключиться к игре"
                Margin="0,0,0,130"  
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Width="200" 
                Height="30" 
                FontSize="18" Click="connectionGameButton_Click"/>
        <Button x:Name="statisticsButton"
                Content="Статистика"
                Margin="0,0,0,50"  
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Width="200" 
                Height="30" 
                FontSize="18" Click="statisticsButton_Click"/>
    </Grid>
</UserControl>
